type Query {
  books: [Book]
  authors: [Author]
}

schema {
  query: Query
}

type Book {
  id: ID!
  title: String
  author: Author 
  authorId: String
}

type Author {
  id: ID!
  name: String
  books: [Book]
}
